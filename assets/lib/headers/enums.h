#include "main.h"

// Enums
typedef enum {
    MAP,
    PLAYER,
    WEAPONS,
    MAP_TOP,
    AllRenderLayers
} RenderLayers;

typedef enum {
    Icon,
    Kungfu,
    BulletTexture,
    AllPaths
} Paths;